# RUN: toy-translate %s -emit=mlir 2>&1 | FileCheck %s

def funcInputs() {
  var a<3,2> = [[1,2],[3,4],[5,6]];
  var b<2,3> = [[1,2,3],[4,5,6]];
}

def multiply(a,b) {
  var c = a * b ;
  print(c);
  # CHECK-LABEL: toy.func @multiply(%arg0: tensor<3x2xf64>, %arg1: tensor<2x3xf64>) {
  # CHECK-NEXT:   %0 = toy.matmul(%arg0, %arg1) : tensor<3x2xf64>, tensor<2x3xf64> -> tensor<3x3xf64>
  # CHECK-NEXT:   toy.print(%0) : tensor<3x3xf64>
  # CHECK-NEXT:   toy.return
  # CHECK-NEXT: }
}
